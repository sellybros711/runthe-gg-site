/* Stub awards dataset; scripts/fetch-awards.mjs populates this via CI.
 * When RTG_AWARDS.players is populated, data.js merges award tags onto
 * matching corpus entities. */
window.RTG_AWARDS = { updated: null, count: 0, players: {} };
